import React from 'react';
import Dashboard from '../components/Dashboard';

const Home = (props) => {
    return (
        <div>
            <Dashboard />
        </div>
    );
};

export default Home;
