import React from 'react';
import Dashboard from '../containers/Dashboard';

const Home = (props) => {
    return (
        <div>
            <Dashboard />
        </div>
    );
};

export default Home;
