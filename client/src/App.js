import React from 'react';
import Routes from './routes/index';

const App = (props) => {
    const { toast, snackbar } = require('tailwind-toast');

    return (
        <Routes />
    );
};

export default App;
